/**
 * Represents a bootstrap list item.
 */
export interface IBsListItem {
     /**
      * The item text.
      */
     itemText: String;
     /**
      * The object to reference.
      */
     item: any;
}
